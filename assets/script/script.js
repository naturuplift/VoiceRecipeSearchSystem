
// Listening state when user record/stop/verify/send audio/text
document.addEventListener('DOMContentLoaded', () => {
    // declare button listeners
    const recordBtn = document.getElementById('record-btn');
    const stopBtn = document.getElementById('stop-btn');
    const verifyBtn = document.getElementById('verify-btn');
    const sendBtn = document.getElementById('send-btn');

});

// using web speech api - speech recognition and speech synthesis


// Fuo verify audio recording

// Send audio recording to process speech recognition with wit.ai






